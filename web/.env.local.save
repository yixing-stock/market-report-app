USE_MOCK_AI=true
const useMock = process.env.USE_MOCK_AI === "true";

if (useMock) {
  return new Response(
    `<h1>Mock Mode</h1><p>Ticker received successfully.</p>`,
    { headers: { "Content-Type": "text/html" } }
  );
}

